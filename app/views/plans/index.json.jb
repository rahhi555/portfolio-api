@plans.map do |plan|
  author = plan.user.nil? ? '退会済み' : plan.user.name
  {
    id: plan.id,
    name: plan.name,
    createdAt: plan.created_at,
    updatedAt: plan.updated_at,
    author: author
  }
end
